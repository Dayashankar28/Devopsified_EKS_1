apiVersion: v1
kind: Service
metadata:
  name: gowebapp-service
  labels:
    app: gowebapp
spec:
  selector:
    app: gowebapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8089
  type: NodePort
